# Copy this file to terraform.tfvars and replace these values.
key_name   = "your-existing-ec2-key-pair"
admin_cidr = "203.0.113.10/32"

# Optional: default ports are 80, 443, 5000, and 8080.
# public_tcp_ports = [80, 443, 8080]
